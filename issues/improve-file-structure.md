### Issue: Improve File Structure

We propose improving the file structure of the project by moving the CRUD operations to a new `services/` directory. This change aims to better align with our business logic. 

#### Proposed Changes:
- Rename or move the `crud/` directory to `services/`.
- Update all references to the old `crud/` directory in the codebase accordingly.

This will facilitate better organization and clarity in our project structure.